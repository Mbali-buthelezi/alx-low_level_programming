#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int m = 65;

	int n = 97;

	while
		(n <= 122) {
			putchar(n);
		
		}
	while
		(m <= 90) {
			putchar(m);
			
		}
	putchar('\n');
	return (0);
}
